extern fun printf(str: u8*, ...) void;


fun main() i32
{

    a: i32 = 0;
    b: i32 = 1;

    for (i: i32 = 0; i < 25; i += 1)
    {
        next: i32 = a + b;
        a = b;
        b = next;
    }

    printf("%d\n\0", a);

    return 0;
}
