extern fun printf(u8* str, ...) void;


fun main() i32
{

    i32 a = 0;
    i32 b = 1;

    for (i32 i = 0; i < 25; i += 1)
    {
        i32 next = a + b;
        a = b;
        b = next;
    }

    printf("%d\n\0", a);

    return 0;
}
