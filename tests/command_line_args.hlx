extern fun printf(str: u8*, ...) void;

fun main(argc: i64, argv: u8**) i32
{
    i: i64 = 0;
    while (i < argc)
    {
        printf("%d: %s\n\0", i, *(argv+8*i));
        i = i + 1;
    }

    return 0;
}

