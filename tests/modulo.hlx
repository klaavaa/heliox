extern fun printf(str: u8*, ...) void;

fun main() i32
{
    i: i32 = 0; 
    while (i < 20)
    {
        if (i % 3 == 0)
        {
            printf("%d\n\0", i);
        }
        i += 1;
    }

    return 0;
}
