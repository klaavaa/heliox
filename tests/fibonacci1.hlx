extern fun printf(str: u8*, ...) void;
fun fibonacci(x: i64) i64
{
    if (x == 0) return 0;
    if (x == 1) return 1;
    return fibonacci(x-1) + fibonacci(x-2);
}

fun main() i64
{
    
    printf("%d\n\0", fibonacci(25));

    return 0;
}
